# Right half uses nice_view which provides its own battery widget.
# Disable the stock battery widget to avoid linker conflicts with
# nice_view's peripheral_status.c (both define widget_battery_status).
CONFIG_ZMK_WIDGET_BATTERY_STATUS=n
